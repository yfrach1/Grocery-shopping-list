import { Link } from "react-router-dom";
import styled from "styled-components";

const SwitchModeLink = styled(Link)`
  color: white;
  font-weight: bolder;
  border-bottom: 2px solid white;
  text-decoration: none;
  /* margin: 10px; */
`;

export default SwitchModeLink;
