import styled from "styled-components";

const Card = styled.div`
  display: flex;
  justify-content: center;
  width: 100%;
  height: 100%;
  height: max-content;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.26);
  padding: 40px 30px;
  overflow: hidden;
  background: rgba(255, 255, 255, 0.2);
  background-color: rgba(82, 165, 91, 0.8);
  text-align: top;

  /* form {
    margin-bottom: 1rem;
  }

  .authentication {
    width: 90%;
    max-width: 25rem;
    margin: 7rem auto;
    text-align: center;
  } */
`;

export default Card;
